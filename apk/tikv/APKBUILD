# Contributor: Watanabe Takashi <wtnb75@gmail.com>
# Maintainer:
pkgname=tikv
pkgver=5.0.2
pkgrel=0
pkgdesc="Distributed transactional key-value database, originally created to complement TiDB"
url="https://tikv.org/"
arch="all"
license="Apache-2.0"
depends=""
makedepends="cargo rust"
install=""
subpackages=""
source="${pkgname}-${pkgver}.tar.gz::https://github.com/tikv/${pkgname}/archive/refs/tags/v${pkgver}.tar.gz"
builddir="${srcdir}/${pkgname}-${pkgver}"

#prepare() {
#	default_prepare
#	rustup-init -y
#}

build() {
	mkdir -p ${builddir}
	cd "$builddir"
	make build_dist_release
}

check() {
	:
}

package() {
	cd "$builddir"
	mkdir -p ${pkgdir}/usr/bin
	cp target/release/tikv-* ${pkgdir}/usr/bin
}
sha512sums="d79d337960fb1e7264b414295be006a9e83c04d069aa53f3337d57c0820fe66fda90662e1c46ff7967f81b9892fae3dae5993828f7450252214523bcfaf4ec60  tikv-5.0.2.tar.gz"
