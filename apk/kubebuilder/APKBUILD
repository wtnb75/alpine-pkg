# Contributor: Watanabe Takashi <wtnb75@gmail.com>
# Maintainer:
pkgname=kubebuilder
pkgver=3.11.1
pkgrel=0
pkgdesc="Kubebuilder - SDK for building Kubernetes APIs using CRDs"
url="https://book.kubebuilder.io/"
arch="all"
license="Apache-2.0"
depends=""
makedepends="go"
install=""
subpackages=""
source="$pkgname-$pkgver.tar.gz::https://github.com/kubernetes-sigs/kubebuilder/archive/refs/tags/v${pkgver}.tar.gz"
builddir="$srcdir/$pkgname-$pkgver"

prepare() {
	default_prepare
	cd "$builddir"
	sed -i -e 's,\$(shell git rev-parse HEAD),v'$pkgver',g;' Makefile
	sed -i -e 's,\$(shell git describe --tags --dirty --broken),v'$pkgver',g;' Makefile
}

build() {
	cd "$builddir"
	make build
}

check() {
	# Replace with proper check command(s)
	:
}

package() {
	cd "$builddir"
	mkdir -p ${pkgdir}/usr/bin
	make install GOBIN=${pkgdir}/usr/bin
}
sha512sums="
7163ae24e4b197e6fe9d2936d9b888168fec57beb453bec961e68bce7a04dd9fcce6818ec06fefb13647efa311e83ae79d76af505661aed187fc041ea0130e9e  kubebuilder-3.11.1.tar.gz
"
