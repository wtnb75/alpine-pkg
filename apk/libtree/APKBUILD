# Contributor: Watanabe Takashi <wtnb75@gmail.com>
# Maintainer:
pkgname=libtree
pkgver=3.0.2
pkgrel=0
pkgdesc="ldd as a tree with an option to bundle dependencies into a single folder"
url="https://github.com/haampie/libtree"
arch="all"
license="MIT"
depends="chrpath binutils"
makedepends="cxxopts elfio termcolor"
install=""
subpackages="$pkgname-doc"
source="$pkgname-$pkgver.tar.gz::https://github.com/haampie/$pkgname/archive/refs/tags/v${pkgver}.tar.gz"
builddir="$srcdir/$pkgname-$pkgver"

build() {
	cd "$builddir"
	mkdir build
	cd build
	cmake .. -DCMAKE_INSTALL_PREFIX=/usr
	make
}

check() {
	# Replace with proper check command(s)
	:
}

package() {
	cd "$builddir"/build
	make install DESTDIR=$pkgdir
}
sha512sums="
db2a8d7e69f1f3ea1c09f9af0739b7a6019cba1eef62d5961a9d0bac2703397b1a91e9d65a04db51bc63e2e4d333c9369311815ef8c2f810ab26f0ba17b1c8db  libtree-3.0.2.tar.gz
"
