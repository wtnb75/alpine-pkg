pypy       # build failed
pyston     # build failed
mitmproxy  # python package dependency
deno       # build failed(rusty_v8, file too small to be an archive)
julia      # build failed
traefik    # build failed(tag? v1 or v2)
abcmidi    # no license
orangefs   # build failed(c macro problem)
corosync   # working
pacemaker  # needs libqb, libcpg(corosync)
cling      # build failed(LLVMDemangle)
ttyrec     # no license
pyenv      # cannot install python
aamath     # site down?
tikv       # build failed(proc-macro vs. musl)
tikv-pd    # live version check problem
mvfst      # dep
hugin      # working
proxygen   # working
wxWidgets  # build failed
youki      # build failed (cfg-if https://github.com/alexcrichton/cfg-if/issues/44)
atsquic    # working
vlang      # build failed(tcc.exe)
fizz       # build failed(folly/Traits.h error: 'value' is not a member of...)
wangle     # need fizz
fbcinder   # build failed
uftrace    # missing gcrt1.o
cdb        # no license
mesh       # build failed
hoard      # build failed
zig        # needs llvm 12.x
gmic       # build failed
mongrel2   # build failed
opentelemetry-collector  # build failed?
glusterfs  # build failed(liburing)
nginx-cfquic # build(prepare) failed
folly      # build failed
ipfs       # go-ipfs
juman      # build failed
jumanpp    # build failed
clair      # build failed(go.mod replace directive)
vitess     # build failed(error obtaining VCS status: exit status 128)
cedro      # build failed(Segmentation fault)
hashlink   # build failed(undefined reference to tgkill)
octosql    # build failed
gifski     # build failed(/usr/include/libavcodec/vaapi.h)
act        # build failed(go.mod replace directive)
boringtun  # yanked from registry
atlas      # build failed
trafficserver # missing execinfo
xsimd      # exists in edge/testing
qsv        # github tag != crates.io version?
docuum     # exists in edge/community
erofs-utils # exists in edge/testing
lzfse       # exists in edge/testing
go-getter  # v2 invalid version
ehco       # cannot use "quic-go doesn't build on Go 1.19 yet."
d2         # undefined reference
easeprobe  # v2 invalid version
alpaca     # v2 invalid version
gotun      # build failed?
mimalloc   # build failed(undeclared variables)
wasmer     # build failed?
wasmtime   # build failed?
imgproxy   # build failed
