boinc-client
picoquic
picotls
hashlink  # x86/x64 only
gifski    # build failed(i8/u8)
wasmer    # too long build time
pushpin   # build failed?(libzmq??)
zurl      # build failed?(libzmq??)
fizz      # long build time
folly     # long build time
duckdb    # long build time
mask      # build failed?(killed)
uutils    # build failed?(killed)
cfonts    # build failed?(cargo)
wasmtime  # build failed?(cargo)
